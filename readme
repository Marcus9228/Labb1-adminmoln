<!-- 

This API is built to add/remove/update/or get pokemons from a database.

To make the application work in docker follow these steps.

If the application build does not generate the DLL successfully write in terminal:
    1:
        dotnet restore Labb2-webbutv-netAPI.csproj
    2:
        dotnet build Labb2-webbutv-netAPI.csproj -c Release
    3: 
        dotnet publish Labb2-webbutv-netAPI.csproj -c Release -o ./publish /p:UseAppHost=false


To connect the app image to the database.
    
Automatically build and connect everything utilizing docker compose:
    enter in terminal: 
        docker-compose up --build
    
to turn stop and remove containers:
    enter in terminal:
        docker-compose down



    Manually build and connect:

    1: Enter following command to pull sql server.
        docker pull mcr.microsoft.com/mssql/server

    2: run this command in the terminal to start the sql server:
        docker run -e "ACCEPT_EULA=Y" -e "SA_PASSWORD=StrongPassw0rd!" -p 1433:1433 --name sql_server -d mcr.microsoft.com/mssql/server
 
 
 
 -->
